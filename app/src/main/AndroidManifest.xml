<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="istg.edu.ec.appEmprendeISTGDev">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EmprendeIstg"
        tools:targetApi="31">

        <activity
            android:name=".ui.activitys.PantallaInicialActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".InicioActivity"
            android:exported="true"
            android:label="@string/title_activity_inicio"
            android:theme="@style/Theme.EmprendeIstg.NoActionBar">

            <!-- FILTRO PARA RECIBIR DEEP LINKS DE PERFIL Y PUBLICACIÓN -->
            <intent-filter tools:ignore="AppLinkUrlError">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <!-- Acepta deep links para perfiles (app://profile/...) -->
                <data
                    android:host="profile"
                    android:scheme="app" />
                <!-- Acepta deep links para publicaciones (app://publication/...) -->
                <data
                    android:host="publication"
                    android:scheme="app" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.activitys.Invitado"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="false" />

        <!--
        NOTA: Las siguientes declaraciones de Fragment como <activity> han sido COMENTADAS.
        Declarar un Fragment en el manifiesto de esta forma es un error y causa que la app falle.
        Se mantienen comentadas como referencia pero no tienen efecto.
        -->
        <!--
        <activity
            android:name=".ui.fragments.PerfilFragment"
            android:exported="false"></activity>
        <activity
            android:name=".ui.fragments.MisNegociosFragment"
            android:exported="false"></activity>
        <activity
            android:name=".ui.fragments.AgregarNegocioFragment"
            android:exported="false"></activity>
        <activity
            android:name=".ui.fragments.OpcionesAdministradorFragment"
            android:exported="false"></activity>
        <activity
            android:name=".ui.fragments.GestionFiltradoBusquedaFragment"
            android:exported="false"></activity>
        <activity
            android:name=".ui.fragments.SolicitudesNegociosFragment"
            android:exported="false"></activity>
        -->
    </application>

</manifest>